eturn new Promise((resolve,reject)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).clear();tx.oncomplete=()=>{db.close();resolve()};tx.onerror=()=>{db.close();reject(tx.error)}})}
function fileExt(mime){return(mime||'').includes('mp4')?'m4a':(mime||'').includes('ogg')?'ogg':(mime||'').includes('wav')?'wav':'webm'}
async function refreshSessionCount(){let list=[];try{list=await allTakes()}catch{}const n=list.length;$('#sessionCount').textContent=n;$('#sessionShare').disabled=!n;$('#sessionShareSheet').disabled=!n;$('#sessionCopyAll').disabled=!list.some(x=>x.transcript);$('#sessionClear').disabled=!n;$('#sessionHint').textContent=n?`${Math.round(list.reduce((a,x)=>a+(x.duration||0),0))} 秒录音 · 可一次分享`:'录完会自动存入，可一次分享'}
function cleanSessionUrls(){sessionUrls.forEach(URL.revokeObjectURL);sessionUrls=[]}
async function openSessionSheet(){cleanSessionUrls();const list=await allTakes();const box=$('#sessionList');if(!list.length)box.innerHTML='<div class="session-empty">还没有录音。完成一题后会自动加入这里。</div>';else box.innerHTML=list.map((x,i)=>{const u=URL.createObjectURL(x.blob);sessionUrls.push(u);return`<article class="session-item"><div class="session-item-top"><span class="session-no">${i+1}</span><div class="session-info"><b>${esc(x.question)}</b><span>${esc((x.part||'').toUpperCase())} · ${fmt(x.duration||0)} · ${new Date(x.createdAt).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</span></div><button class="session-delete" data-session-delete="${esc(x.id)}">删除</button></div><audio controls preload="metadata" src="${u}"></audio><div class="session-transcript">${x.transcript?esc(x.transcript):'本题没有浏览器转写，音频仍会包含在资料包中。'}</div></article>`}).join('');$('#sessionSheet').hidden=false;document.body.classList.add('sheet-open');await refreshSessionCount()}
function closeSessionSheet(){cleanSessionUrls();$('#sessionSheet').hidden=true;document.body.classList.remove('sheet-open')}
function combinedTranscript(list){return`请按题逐条分析以下 IELTS Speaking 练习。重点看：是否真正回答问题、内容是否断线、长停顿、重复、语法和更容易现场说的改法。\n\n${list.map((x,i)=>`## ${i+1}. ${x.part?.toUpperCase()||''} — ${x.question}\n时长：${fmt(x.duration||0)}；长停顿计数：${x.pauses||0}\n转写：${x.transcript||'[无自动转写，请听音频]'}`).join('\n\n')}`}
async function copyAllTranscripts(){const list=await allTakes();if(!list.length)return;try{await navigator.clipboard.writeText(combinedTranscript(list));toast('已复制全部题目和转写')}catch{toast('复制失败')}}
function u16(n){const a=new Uint8Array(2);new DataView(a.buffer).setUint16(0,n,true);return a}function u32(n){const a=new Uint8Array(4);new DataView(a.buffer).setUint32(0,n>>>0,true);return a}
let crcTable=null;function crc32(bytes){if(!crcTable){crcTable=new Uint32Array(256);for(let n=0;n<256;n++){let c=n;for(let k=0;k<8;k++)c=(c&1)?0xedb88320^(c>>>1):c>>>1;crcTable[n]=c>>>0}}let c=0xffffffff;for(const b of bytes)c=crcTable[(c^b)&255]^(c>>>8);return(c^0xffffffff)>>>0}
function concatBytes(parts){const len=parts.reduce((a,x)=>a+x.length,0),out=new Uint8Array(len);let o=0;for(const x of parts){out.set(x,o);o+=x.length}return out}
function zipTime(d){return[((d.getHours()<<11)|(d.getMinutes()<<5)|(d.getSeconds()>>1))&0xffff,(((d.getFullYear()-1980)<<9)|((d.getMonth()+1)<<5)|d.getDate())&0xffff]}
async function makeZip(files){const enc=new TextEncoder(),locals=[],centrals=[];let offset=0;for(const f of files){const name=enc.encode(f.name),data=new Uint8Array(await f.blob.arrayBuffer()),crc=crc32(data),[tm,dt]=zipTime(new Date(f.date||Date.now()));const local=concatBytes([u32(0x04034b50),u16(20),u16(0x0800),u16(0),u16(tm),u16(dt),u32(crc),u32(data.length),u32(data.length),u16(name.length),u16(0),name,data]);locals.push(local);const central=concatBytes([u32(0x02014b50),u16(20),u16(20),u16(0x0800),u16(0),u16(tm),u16(dt),u32(crc),u32(data.length),u32(data.length),u16(name.length),u16(0),u16(0),u16(0),u16(0),u32(0),u32(offset),name]);centrals.push(central);offset+=local.length}const centralSize=centrals.reduce((a,x)=>a+x.length,0),end=concatBytes([u32(0x06054b50),u16(0),u16(0),u16(files.length),u16(files.length),u32(centralSize),u32(offset),u16(0)]);return new Blob([...locals,...centrals,end],{type:'application/zip'})}
function safeName(s){return String(s||'question').replace(/[\\/:*?"<>|]+/g,'-').replace(/\s+/g,' ').trim().slice(0,54)}
async function makeSessionPackage(){const list=await allTakes();if(!list.length)throw Error('empty');const text=combinedTranscript(list),manifest=JSON.stringify({createdAt:new Date().toISOString(),count:list.length,items:list.map(({blob,...x})=>x)},null,2),files=[{name:'IELTS-speaking-transcripts.txt',blob:new Blob([text],{type:'text/plain;charset=utf-8'})},{name:'manifest.json',blob:new Blob([manifest],{type:'application/json'})}];list.forEach((x,i)=>files.push({name:`${String(i+1).padStart(2,'0')}_${x.part?.toUpperCase()||'P'}_${safeName(x.question)}.${fileExt(x.mime)}`,blob:x.blob,date:x.createdAt}));const zip=await makeZip(files);return new File([zip],`IELTS-speaking-${new Date().toISOString().slice(0,10)}-${list.length}题.zip`,{type:'application/zip'})}
async function shareSession(){const list=await allTakes();if(!list.length){toast('本轮还没有录音');return}toast('正在整理练习包…');try{const file=await makeSessionPackage();const data={files:[file],title:'IELTS Speaking 练习包',text:'请逐题分析我的回答、停顿、逻辑和表达。'};if(navigator.share&&navigator.canShare?.({files:[file]})){await navigator.share(data);toast('已打开系统分享菜单')}else{const url=UR